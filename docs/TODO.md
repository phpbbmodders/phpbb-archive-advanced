# TODO

Ideas not yet built, practical and speculative alike.

- **`--redirect-format {caddy,traefik}`**: extend the old-URL redirect
  generator to also support Caddy and Traefik, matching the existing
  `apache`/`nginx` formats' behavior (including topic/forum pagination
  redirects). Deferred pending real Caddy/Traefik verification. Detailed
  plan: [`docs/TODO/todo-redirect-format-caddy-traefik.md`](TODO/todo-redirect-format-caddy-traefik.md).
